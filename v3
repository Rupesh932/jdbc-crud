move input related method,scanner,closeResource()   to InputManager(new) class from menu managaer class and make them static.
call closeresource() from main after program execution, before it has been call inside mm.method to close scan.
since introduce inputManager class we don't need to pass mm object via constructor to crudmanager ,so delete 
constructor and variable manager from all class cm,ci.
move getcoltype() and validName() to CrudHelper(new) class. from menu manager class.
move getcolvalue() and nullManager() to CrudHelper from CrudManager.
createTable() of ci is totally changed, now table name is taking from cm(if exitsted, else not come to ci), and to get col-name and 
col-constrains add one prepareColumnMeta() to CrudHelper class which return map of col-name and col-constrains.which is also manage by  
cm class and pass dbname,tname,and map to ci from where directly pass those value to cf.cretaeTable().

now new changes to cm.createtable() method. it had too long logic in one place but now keep them separate from single responsible method.
introduce GetName class to get db/table name with prompt or without prompt msg and can be scalable if needed, and introduce 
CrudManagerHelper class and now it has method to create table iftablenotexit and ifdbnotexit but scalable .

next: "Centralized primitive integers via IntConstant class under Constants to improve reuse and readability."
 next: split connectionfactory(large class) into 5 classes.rename curdmanagerhelper as crudflowhelper.
 next: change CrudManagerHelper to CrudFlowHelper.
 next: add several method related to read: readall,readOneRowById,readOneRowByCrenditial in curd interface and implemented them to ci class .
 next: introduce DisplayHleper class to help to print data from map or list which is comming(in chain) from after fire select * from ... qry.
 next: add ReadMode enum to Constants class.
 next: implements handleReadData() method in CrudManager class.
 
 some todo1. map out the dbname and corresponding table into map for all db and table to show before enter db/tb name, user can see and enters
             which db and tb ,without confusing. 
       todo2. develop batch insert, update,delete even create table too .  
           
           done todo1: show db and table list before user enter their name.
 next done: remove MessageStyler class and introduce new StyledMessage class with centralize contorl over bg+emoji+fg+msg   
 		  : Handles all styled output (success, error, warning, input prompts...)     
 		  
next done: remove GetName class and move its renamed and refactured method  to inputmanager to get valid db/tb or new db/tb.
		 :  isDatabaseExit() of DbConnectionManger  only use to get quick result(true/false) if project 
		   needs, otherwise newly introduced method dbNameInput() can handle all posibilities.
	   
	    
next done: combine Constants.enum OperationStatus and OperationMessage to QueryStatus and added getCode() method and code param to constructor and introduce 
		 : toMap() method which return map of code and message. which bind key(code) with value(message) and return if needed.and made changes according 
		 : to newly combined enum types to associated method. and use map.putAll(QueryStatus.ERROR(x).toMap) somewhere.
	    
 